package fundamentos

fun main(args: Array<String>) {
    var a = 1;
    var b = 2;


    /* Da forma como está o código abaixo, ocorrerá um problema porque não informamos que a variável
     * é do tipo double.
     * */
     //a = 2.3;
     print(a+b);
}